import always from './an-always/an-always'
import arity from './an-arity/an-arity'
import compose from './a-compose/a-compose'
import curry from './a-curry/a-curry'
import identity from './an-identity/an-identity'
import map from './a-map/a-map'
import reduce from './a-reduce/a-reduce'

export default {
  always,
  arity,
  compose,
  curry,
  identity,
  map,
  reduce
}
