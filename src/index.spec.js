import always from './an-always/example'
import arity from './an-arity/example'
import compose from './a-compose/example'
import curry from './a-curry/example'
import identity from './an-identity/example'
import map from './a-map/example'
import reduce from './a-reduce/example'

describe('a-sweet-lib', function () {
  it('always', always)
  it('arity', arity)
  it('compose', compose)
  it('curry', curry)
  it('identity', identity)
  it('map', map)
  it('reduce', reduce)
})
